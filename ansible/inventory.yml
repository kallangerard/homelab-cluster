production:
  children:
    masters:
      hosts:
        charles:
    workers:
      hosts:
        kingston:
        moss:
  vars:
    # Vars for production go here
    ansible_user: node
    cluster_cidr: {{ lookup ('env', 'cluster_cidr') }}
