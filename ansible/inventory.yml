production:
  children:
    masters:
      hosts:
        charles:
    workers:
      hosts:
        kingston:
        moss:
  vars:
    # Vars for production go here
    ansible_user: node
    pod_network_cidr: "{{ lookup ('env', 'POD_NETWORK_CIDR') }}"
